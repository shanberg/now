{
  "imports": {
    "std/": "https://deno.land/std@0.189.0/",
    "src/": "./src/"
  },
  "tasks": {
    "dev": "deno run --allow-read --allow-write --watch src/index.ts tui",
    "cli": "deno run --allow-read --allow-write --watch src/index.ts",
    "lint": "deno lint",
    "lint:watch": "deno lint --watch",
    "fmt": "deno fmt",
    "fmt:watch": "deno fmt --watch",
    "test": "deno test tests/",
    "test:watch": "deno test --watch tests/",
    "bundle": "deno run --allow-read --allow-write --allow-env src/bundle.ts",
    "bundle:watch": "deno run --allow-read --allow-write --allow-env --watch src/bundle.ts"
  }
}
